# Requirements Archive: Mini Campaign Manager v1.0

**Archived:** 2026-04-22
**Status:** ✅ ALL SHIPPED — v1.0 MVP
**Coverage:** 51/51 v1 requirements implemented (verified via live stack + backend test suite)

> This is the frozen requirements snapshot for v1.0. Checkboxes reflect final shipping state.
> Deviations from the original spec are noted inline.

---

# Requirements: Mini Campaign Manager

**Defined:** 2026-04-20
**Core Value:** Server-side business-rule correctness and clean, testable architecture — proven by tests and narrated through transparent AI collaboration.

## v1 Requirements

### Foundation & Infra

- [x] **FOUND-01**: Yarn-workspaces monorepo (Yarn 4, `nodeLinker: node-modules`) with `backend/`, `frontend/`, `shared/` workspaces; `shared/` compiles to `dist/` via `tsc`; root `postinstall` builds `shared` so downstream workspaces can import its types
- [x] **FOUND-02**: `docker compose up` starts the **full stack** — Postgres and Redis with healthchecks, then the API container (multi-stage Dockerfile; runs `yarn sequelize db:migrate` before server boot), then the **web container** (multi-stage Dockerfile: node builder → `nginx:alpine`). API waits on `condition: service_healthy` for both db+redis; web waits on `condition: service_started` for api. *(Phase 10)*
- [x] **FOUND-03**: nginx in the web container serves the compiled SPA with `try_files $uri /index.html` fallback and reverse-proxies `/api/*` and `/track/*` to the API container. The web container is the **only** port bound to the host (`8080:80`); reviewer opens `http://localhost:8080` and everything just works (no CORS, no `VITE_API_URL` baked in at build time). *(Phase 10 — nginx also rewrites cookie path `/auth` → `/api/auth` via `proxy_cookie_path`)* ✓ verified live
- [x] **FOUND-04**: Root-level TypeScript + ESLint + Prettier config extended by each workspace
- [x] **FOUND-05**: Pino structured logging wired into the API (request logger + error logger)

### Data Layer

- [x] **DATA-01**: Sequelize models for `User`, `Campaign`, `Recipient`, `CampaignRecipient` with campaign status ENUM `draft | scheduled | sending | sent` and recipient status ENUM `pending | sent | failed`
- [x] **DATA-02**: Sequelize migrations create all tables, FK cascades, and indexes:
  - `CREATE EXTENSION IF NOT EXISTS pgcrypto;` (first migration — needed for `gen_random_uuid()`)
  - `campaign_recipients.tracking_token UUID UNIQUE NOT NULL DEFAULT gen_random_uuid()` — public-facing token for the open-tracking pixel URL (defeats enumeration since the natural composite PK is BIGINT)
  - `(created_by, created_at DESC, id DESC)` on campaigns — covers cursor pagination + ownership filter in a single B-tree scan
  - `(campaign_id, status)` on campaign_recipients — covers stats aggregation
  - `tracking_token` UNIQUE on campaign_recipients — covers pixel lookup
  - Composite PK `(campaign_id, recipient_id)` on campaign_recipients — covers worker update path
  - UNIQUE index on `users.email` and `recipients.email`
  - FK cascade: `campaign_id` `ON DELETE CASCADE` so deleting a draft campaign cleans up its recipient links
- [x] **DATA-03**: Seed script creates one demo user, ten recipients, one draft + one scheduled + one sent campaign for walkthrough

### Authentication

- [x] **AUTH-01**: User can register via `POST /auth/register` — email uniqueness enforced, password bcrypt-hashed ✓ verified live
- [x] **AUTH-02**: User can login via `POST /auth/login` — returns access token in response body (short TTL, kept in memory) and sets refresh token in httpOnly + SameSite cookie ✓ verified live
- [x] **AUTH-03**: User can rotate tokens via `POST /auth/refresh` using the refresh cookie ✓ verified via backend tests + static analysis
- [x] **AUTH-04**: User can logout via `POST /auth/logout` — revokes refresh token via Redis denylist and clears cookie ✓ verified via static analysis (22/22 verification truths)
- [x] **AUTH-05**: `GET /auth/me` returns the authenticated user (used by frontend to rehydrate session after refresh) ✓ verified live
- [x] **AUTH-06**: JWT middleware guards `/campaigns/*` and `/recipients/*` — 401 on missing/invalid token ✓ verified live (no-token → 401) + TEST-04
- [x] **AUTH-07**: Users can only access campaigns they created — cross-user access returns 404 (not 403, to avoid enumeration) ✓ verified live + TEST-04

### Campaigns API

- [x] **CAMP-01**: `GET /campaigns` lists campaigns for the authed user with pagination. *Deviation: offset pagination (`?page=&limit=`) instead of cursor — documented override in `docs/DECISIONS.md`. Rationale: page-number UI incompatible with cursor semantics. Frontend matches backend implementation.* ✓ verified live
- [x] **CAMP-02**: `POST /campaigns` creates a campaign in `draft` with `name`, `subject`, `body`, `recipientEmails[]`; upserts recipients; links via `CampaignRecipient` rows in `pending` state ✓ verified live (draft created, stats total=2 for 2 recipients)
- [x] **CAMP-03**: `GET /campaigns/:id` returns campaign with eager-loaded recipients (no N+1) and inline aggregate stats ✓ verified live
- [x] **CAMP-04**: `PATCH /campaigns/:id` updates editable fields — **409 Conflict** if status ≠ draft ✓ verified via TEST-01 (11/11 backend tests pass)
- [x] **CAMP-05**: `DELETE /campaigns/:id` — **409 Conflict** if status ≠ draft; cascades to `CampaignRecipient` ✓ verified via TEST-01
- [x] **CAMP-06**: `POST /campaigns/:id/schedule` sets `scheduled_at` and enqueues a **delayed** BullMQ job — 400 if `scheduled_at` is not in the future, 409 if status ≠ draft ✓ verified via static analysis + smoke scripts
- [x] **CAMP-07**: `POST /campaigns/:id/send` transitions campaign `draft|scheduled → sending` via atomic `UPDATE ... WHERE status IN ('draft','scheduled')`, enqueues immediate BullMQ job, returns **202 Accepted** — 409 if the atomic guard matches zero rows ✓ verified live (concurrent send: 202 + 409) + TEST-02
- [x] **CAMP-08**: `GET /campaigns/:id/stats` returns `{ total, sent, failed, opened, open_rate, send_rate }` computed via a single aggregate SQL query (no JS counting) ✓ verified live + TEST-03

### Recipients API

- [x] **RECIP-01**: `POST /recipients` creates or upserts a recipient (INSERT … ON CONFLICT on email). *Minor deviation: path is `/recipients` (plural) per REST convention, not `/recipient` (singular) as written in spec — REST convention preferred.* ✓ verified live
- [x] **RECIP-02**: `GET /recipients` lists recipients (cursor paginated) ✓ verified via static analysis

### Async Sending (BullMQ)

- [x] **QUEUE-01**: BullMQ `Queue` and `Worker` wired to Redis with **separate IORedis connections**, both using `maxRetriesPerRequest: null` ✓ verified via static analysis (lib/queue.ts)
- [x] **QUEUE-02**: Send worker processes a campaign inside a Sequelize transaction: randomly marks each `CampaignRecipient` as `sent` or `failed`, sets `sent_at` for `sent`, then transitions campaign to `sent` ✓ verified live (campaign status='sent' after 10s, stats total=sent+failed) + TEST-02
- [x] **QUEUE-03**: Delayed send jobs for scheduled campaigns converge to the same worker; worker re-checks campaign status and bails if no longer in `sending` ✓ verified via static analysis (sendWorker.ts status re-check)
- [x] **QUEUE-04**: Worker registers `failed` and `error` event handlers that log via pino (no silent failures) ✓ verified via static analysis

### Open Tracking

- [x] **TRACK-01**: `GET /track/open/:trackingToken` (public, no auth) returns the canonical 43-byte transparent GIF89a and runs `UPDATE campaign_recipients SET opened_at = NOW() WHERE tracking_token = $1 AND opened_at IS NULL` (idempotent — first open wins, subsequent matches zero rows). **Always returns 200 + GIF** even if the token doesn't match any row (oracle-attack defense). Headers: `Content-Type: image/gif`, `Cache-Control: no-store, no-cache`, `Referrer-Policy: no-referrer`. Pixel buffer is module-scoped, not read per-request. ✓ verified live (200+43bytes+headers for valid/invalid/garbage tokens; idempotency: opened count stays 1 after 3 hits)

### Frontend

- [x] **UI-01**: Vite + React 18 + TS app with Tailwind + shadcn/ui, Redux Toolkit store, and React Query provider configured ✓ verified (docker stack serves SPA at localhost:8080)
- [x] **UI-02**: `/login` page — form POSTs to `/auth/login`; access token stored in Redux memory; relies on httpOnly refresh cookie ✓ verified live
- [x] **UI-03**: App bootstrap — on load, calls `/auth/refresh` then `/auth/me` to rehydrate session after page refresh ✓ verified via source (useBootstrap hook)
- [x] **UI-04**: Route guard — redirects unauthenticated users to `/login`; preserves return-to URL ✓ verified via source (ProtectedRoute.tsx)
- [x] **UI-05**: HTTP client — injects access token, transparently refreshes once on 401 then retries, clears session on persistent auth failure ✓ verified via source (apiClient.ts interceptor + 10.1-01 fix)
- [x] **UI-06**: `/campaigns` list — offset pagination via React Query `useInfiniteQuery` (approved deviation: offset over cursor, per Phase 4 decision), status badges (`draft=grey`, `scheduled=blue`, `sending=amber w/ spinner`, `sent=green`), skeleton loaders, empty state, "New campaign" action ✓ verified live
- [x] **UI-07**: `/campaigns/new` — Zod-validated form for name / subject / body / recipient-email tokenizer; `POST /campaigns`; redirect to detail on success ✓ verified live
- [x] **UI-08**: `/campaigns/:id` detail — `send_rate` and `open_rate` progress bars, per-recipient status list, conditional action buttons based on status ✓ verified live
- [x] **UI-09**: Schedule action — date/time picker (timezone-aware), `POST /campaigns/:id/schedule` ✓ verified live
- [x] **UI-10**: Send action — confirm dialog, `POST /campaigns/:id/send`; while status is `sending`, stats + detail queries refetch every 2s, stopping on `sent` ✓ verified live (Phase 10.2 fixed infinite polling)
- [x] **UI-11**: Delete action — confirm dialog, `DELETE /campaigns/:id` ✓ verified live (Phase 10.2 fixed 409 bug)
- [x] **UI-12**: Global error handling — React Query error boundary + toast notifications that surface API error messages; skeleton loaders during fetches ✓ verified live (Phase 10.2 added meaningful toasts)
- [x] **UI-13**: Logout — `POST /auth/logout`, clears Redux auth, redirects to `/login` ✓ verified live

### Testing

- [x] **TEST-01**: Vitest + Supertest — status-guard test: `PATCH`/`DELETE`/`/send` on a non-draft campaign return 409 ✓ 3/3 assertions pass (11/11 total tests)
- [x] **TEST-02**: Vitest + Supertest — send atomicity: two parallel `POST /send` calls on the same draft result in exactly one 202 and one 409 ✓ passes + verified live (concurrent curl also 202+409)
- [x] **TEST-03**: Vitest + Supertest — stats aggregation: seeded data produces correct counts and rates from `GET /campaigns/:id/stats` ✓ 3/3 assertions pass
- [x] **TEST-04**: Vitest + Supertest — auth middleware: 401 on missing token, 401 on invalid token, cross-user access returns 404 ✓ 4/4 assertions pass
- [x] **TEST-05**: Vitest + @testing-library/react — `CampaignBadge` renders correct color/label per status (incl. `sending`) ✓ passes

### Documentation

- [x] **DOC-01**: Root README — project overview, one-command `docker compose up` setup, demo login, env vars, how to run tests, optional HMR developer flow ✓ shipped in Phase 10
- [x] **DOC-02**: "How I Used Claude Code" section — 2-3 real prompts, 1-2 concrete corrections, explicit list of what Claude Code was NOT allowed to do and why — assembled from live log ✓ shipped in Phase 10 (user-reviewed checkpoint)
- [x] **DOC-03**: `docs/DECISIONS.md` — 4-state machine rationale, index choices, async queue rationale, open-tracking design, JWT split rationale ✓ shipped across phases

## v2 Requirements

Deferred — tracked but not in v1.0 roadmap.

### Delivery
- **V2-DELIV-01**: Real SMTP delivery via SES/Mailgun
- **V2-DELIV-02**: Send cancellation / retry semantics
- **V2-DELIV-03**: Unsubscribe link + preference management

### Composition
- **V2-COMP-01**: Rich-text / HTML email editor
- **V2-COMP-02**: Template library
- **V2-COMP-03**: Recipient CSV import
- **V2-COMP-04**: Segments and tags

### Observability
- **V2-OBS-01**: Real-time send progress via WebSocket (instead of polling)
- **V2-OBS-02**: Metrics + tracing (OpenTelemetry)

### Platform
- **V2-PLAT-01**: OAuth / social login
- **V2-PLAT-02**: Multi-tenant workspaces + RBAC
- **V2-PLAT-03**: Dark mode
- **V2-PLAT-04**: Fully dockerized web (not just API)
- **V2-PLAT-05**: CI/CD pipeline + deployments

## Traceability

| Requirement | Phase | Status |
|-------------|-------|--------|
| FOUND-01 | Phase 1 | ✓ Complete |
| FOUND-02 | Phase 10 | ✓ Complete |
| FOUND-03 | Phase 10 | ✓ Complete |
| FOUND-04 | Phase 1 | ✓ Complete |
| FOUND-05 | Phase 1 | ✓ Complete |
| DATA-01 | Phase 2 | ✓ Complete |
| DATA-02 | Phase 2 | ✓ Complete |
| DATA-03 | Phase 2 | ✓ Complete |
| AUTH-01 | Phase 3 | ✓ Complete |
| AUTH-02 | Phase 3 | ✓ Complete |
| AUTH-03 | Phase 3 | ✓ Complete |
| AUTH-04 | Phase 3 | ✓ Complete |
| AUTH-05 | Phase 3 | ✓ Complete |
| AUTH-06 | Phase 3 | ✓ Complete |
| AUTH-07 | Phase 3 | ✓ Complete |
| CAMP-01 | Phase 4 | ✓ Complete (offset pagination deviation — documented) |
| CAMP-02 | Phase 4 | ✓ Complete |
| CAMP-03 | Phase 4 | ✓ Complete |
| CAMP-04 | Phase 4 | ✓ Complete |
| CAMP-05 | Phase 4 | ✓ Complete |
| CAMP-06 | Phase 5 | ✓ Complete |
| CAMP-07 | Phase 5 | ✓ Complete |
| CAMP-08 | Phase 4 | ✓ Complete |
| RECIP-01 | Phase 4 | ✓ Complete (path is /recipients plural — minor spec deviation) |
| RECIP-02 | Phase 4 | ✓ Complete |
| QUEUE-01 | Phase 5 | ✓ Complete |
| QUEUE-02 | Phase 5 | ✓ Complete |
| QUEUE-03 | Phase 5 | ✓ Complete |
| QUEUE-04 | Phase 5 | ✓ Complete |
| TRACK-01 | Phase 6 | ✓ Complete |
| UI-01 | Phase 8 | ✓ Complete |
| UI-02 | Phase 9 | ✓ Complete |
| UI-03 | Phase 8+10.1 | ✓ Complete |
| UI-04 | Phase 8+10.1 | ✓ Complete |
| UI-05 | Phase 8+10.1 | ✓ Complete |
| UI-06 | Phase 9 | ✓ Complete |
| UI-07 | Phase 9 | ✓ Complete |
| UI-08 | Phase 9+10.2 | ✓ Complete |
| UI-09 | Phase 9 | ✓ Complete |
| UI-10 | Phase 9+10.2 | ✓ Complete |
| UI-11 | Phase 9+10.2 | ✓ Complete |
| UI-12 | Phase 9+10.2 | ✓ Complete |
| UI-13 | Phase 9 | ✓ Complete |
| TEST-01 | Phase 7 | ✓ Complete |
| TEST-02 | Phase 7 | ✓ Complete |
| TEST-03 | Phase 7 | ✓ Complete |
| TEST-04 | Phase 7 | ✓ Complete |
| TEST-05 | Phase 9 | ✓ Complete |
| DOC-01 | Phase 10 | ✓ Complete |
| DOC-02 | Phase 10 | ✓ Complete |
| DOC-03 | Phase 10 | ✓ Complete |

**Final Coverage: 51/51 v1 requirements — 100%**

Known deviations (documented and accepted):
- CAMP-01: offset pagination instead of cursor (docs/DECISIONS.md)
- RECIP-01: `/recipients` plural path vs `/recipient` singular in spec

---
*Archived: 2026-04-22 at v1.0 milestone close*
