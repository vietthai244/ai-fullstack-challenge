# Phase 10.1: UAT Fixes — Auth, Navigation & Register - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-22
**Phase:** 10.1 — UAT Fixes — Auth, Navigation & Register
**Areas discussed:** Navigation design, Register form fields, Login↔Register linking, Auth bug fix approach, Nav bar on auth pages, Logout behavior

---

## Navigation design

| Option | Description | Selected |
|--------|-------------|----------|
| Top nav bar | Persistent header on all protected pages | ✓ |
| Per-page back buttons only | Each page has its own back arrow | |
| Inline links within page content | Clickable rows, no nav component | |

**User's choice:** Top nav bar (recommended)
**Notes:** App title left, Campaigns link + Logout button right.

---

## Nav bar content

| Option | Description | Selected |
|--------|-------------|----------|
| App title + Campaigns link + Logout | Standard nav with link and action | ✓ |
| App title + user email + Logout | Shows who's logged in, no nav link | |

**User's choice:** App title + Campaigns link + Logout (recommended)

---

## Register form fields

| Option | Description | Selected |
|--------|-------------|----------|
| Email + Password + Name | All 3 fields from RegisterSchema | ✓ |
| Email + Password only | Skip name field | |

**User's choice:** Email + Password + Name (recommended)
**Notes:** Name is first field in the form.

---

## Confirm password field

| Option | Description | Selected |
|--------|-------------|----------|
| No confirm password | Keep it simple | ✓ |
| Yes — add confirm password | Client-side refine() validation | |

**User's choice:** No — keep it simple (recommended)

---

## Login↔Register link placement

| Option | Description | Selected |
|--------|-------------|----------|
| Below the form card as plain text link | Centered text below Card | ✓ |
| Inside the card footer | Within CardContent at bottom | |

**User's choice:** Below the form card as plain text link (recommended)

---

## After registration destination

| Option | Description | Selected |
|--------|-------------|----------|
| Auto-login and redirect to /campaigns | Dispatch setAuth, navigate to /campaigns | ✓ |
| Redirect to /login with success message | Extra step, simpler code | |

**User's choice:** Auto-login and redirect to /campaigns (recommended)

---

## Auth bug fix approach

| Option | Description | Selected |
|--------|-------------|----------|
| URL allowlist — skip retry for auth endpoints | Check URL before retry | ✓ |
| Separate axios instance for auth calls | New instance, refactor callers | |

**User's choice:** URL allowlist (recommended)
**Notes:** Skip interceptor retry if URL includes `/auth/login`, `/auth/refresh`, `/auth/register`.

---

## Nav bar on login/register pages

| Option | Description | Selected |
|--------|-------------|----------|
| No — nav only on protected pages | Standalone auth pages, no nav | ✓ |
| Yes — nav on all pages | App name shown everywhere | |

**User's choice:** No — nav only on protected pages (recommended)

---

## Logout destination

| Option | Description | Selected |
|--------|-------------|----------|
| Redirect to /login immediately | Clean, no extra feedback | |
| Redirect to /login with a toast message | "You've been logged out" toast | ✓ |

**User's choice:** Redirect to /login with a toast message

---

## Logout confirmation dialog

| Option | Description | Selected |
|--------|-------------|----------|
| No dialog — immediate logout | Single click logs out | ✓ |
| Yes — confirm before logout | AlertDialog before clear | |

**User's choice:** No dialog — immediate logout (recommended)

---

## Claude's Discretion

- Exact ProtectedLayout vs inline NavBar implementation approach
- NavBar styling (padding, height, border) — follow existing Tailwind conventions
- Logout button variant (ghost or outline)
