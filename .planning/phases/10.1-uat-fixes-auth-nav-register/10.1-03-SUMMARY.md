---
plan: "10.1-03"
title: "RegisterPage + App.tsx wiring + login link"
status: complete
completed: "2026-04-22"
commit: "2f37794"
---

## What Was Built

Three coordinated changes completing the phase:

1. **RegisterPage.tsx** — mirrors LoginPage structure with name/email/password fields, RegisterSchema validation, auto-login on success (dispatch setAuth → navigate('/campaigns')), "Log in" cross-link below card.
2. **App.tsx** — added `/register` public route, introduced `ProtectedLayout` inline component (NavBar above `<main>`), wrapped all three protected routes with ProtectedLayout.
3. **LoginPage.tsx** — added `Link` to react-router-dom import, "Don't have an account? Register" cross-link below Card.

## Key Files

### Created
- `frontend/src/pages/RegisterPage.tsx`

### Modified
- `frontend/src/App.tsx` — /register route + ProtectedLayout + NavBar wiring
- `frontend/src/pages/LoginPage.tsx` — register cross-link

## Self-Check: PASSED

| Criterion | Result |
|-----------|--------|
| `export function RegisterPage` | ✓ 1 match |
| `auth/register` mutationFn | ✓ 1 match |
| `Create your account` CardTitle | ✓ 1 match |
| `Already have an account` cross-link text | ✓ 1 match |
| `to="/login"` in RegisterPage | ✓ 1 match |
| `id="name"` field | ✓ 1 match |
| `navigate('/campaigns')` auto-login | ✓ (code + comment) |
| `import.*NavBar` in App.tsx | ✓ 1 match |
| `import.*RegisterPage` in App.tsx | ✓ 1 match |
| `ProtectedLayout` in App.tsx | ✓ 7 occurrences (definition + 3×open + 3×close) |
| `path="/register"` | ✓ 1 match |
| `<NavBar` inside ProtectedLayout | ✓ 1 match |
| `Don't have an account` in LoginPage | ✓ 1 match |
| `to="/register"` in LoginPage | ✓ 1 match |
| `yarn workspace @campaign/frontend typecheck` | ✓ exits 0 |
| `yarn workspace @campaign/frontend build` | ✓ exits 0, 1815 modules |

## Issues Encountered

None. All three tasks executed cleanly with passing typecheck and Vite build.
