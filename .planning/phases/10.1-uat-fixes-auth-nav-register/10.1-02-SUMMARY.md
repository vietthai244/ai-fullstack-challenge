---
plan: "10.1-02"
title: "NavBar component for protected routes"
status: complete
completed: "2026-04-22"
commit: "1db4edb"
---

## What Was Built

Created `frontend/src/components/NavBar.tsx` — a top navigation bar rendered inside all protected routes via ProtectedLayout (wired in Wave 3). Provides app-level navigation and logout.

## Key Files

### Created
- `frontend/src/components/NavBar.tsx` — NavBar component with app title link, Campaigns link, and Logout button

## Behaviors

- Left: "Campaign Manager" heading as `<Link to="/campaigns">` (D-05)
- Right: Campaigns nav link + Logout ghost button (D-05)
- Logout: POST /api/auth/logout (best-effort) → `dispatch(clearAuth())` → `toast(...)` → `navigate('/login')`
- Non-fatal logout: local state cleared even if server call fails (T-10.1-03)

## Self-Check: PASSED

| Criterion | Result |
|-----------|--------|
| `export function NavBar` present | ✓ |
| `clearAuth` import + dispatch call | ✓ (2 occurrences) |
| `You've been logged out` toast | ✓ |
| `post('/auth/logout')` call | ✓ |
| `to="/campaigns"` ×2 (title + nav link) | ✓ |
| `navigate('/login')` | ✓ |
| `yarn workspace @campaign/frontend typecheck` | ✓ exits 0 |

## Issues Encountered

None.
