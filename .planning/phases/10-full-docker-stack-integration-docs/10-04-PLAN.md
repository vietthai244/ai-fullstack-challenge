---
phase: 10-full-docker-stack-integration-docs
plan: 04
type: execute
wave: 3
depends_on:
  - "10-03"
files_modified:
  - docs/DECISIONS.md
  - README.md
autonomous: false
requirements:
  - DOC-01
  - DOC-02
  - DOC-03

must_haves:
  truths:
    - "docs/DECISIONS.md has 4 new sections covering the required topics"
    - "README.md exists with one-command setup, demo creds, env vars, test instructions, HMR dev flow"
    - "README.md How I Used Claude Code section reviewed and approved by user before commit"
  artifacts:
    - path: "docs/DECISIONS.md"
      provides: "4-state machine, index choices, async queue, open-tracking rationale"
      contains: "4-State Campaign Machine"
    - path: "README.md"
      provides: "Reviewer-facing project README with Claude Code transparency section"
      contains: "docker compose up"
  key_links:
    - from: "README.md"
      to: "docs/DECISIONS.md"
      via: "cross-reference link"
      pattern: "docs/DECISIONS.md"
    - from: "README.md How I Used Claude Code"
      to: "user approval"
      via: "checkpoint:human-verify gate"
      pattern: "approved"
---

<objective>
Write the four missing docs/DECISIONS.md sections and create README.md. The "How I Used Claude Code" section of README.md must be drafted from the actual build log and reviewed by the user before committing.

Purpose: DOC-01 (README), DOC-02 ("How I Used Claude Code"), DOC-03 (DECISIONS.md) are the final deliverables for the take-home reviewer. Authenticity and completeness are required.

Output: Updated `docs/DECISIONS.md`, new `README.md` (committed only after user approves the Claude Code section).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-full-docker-stack-integration-docs/10-03-SUMMARY.md

<interfaces>
<!-- docs/DECISIONS.md currently has 3 sections: -->
<!-- 1. JWT: refresh cookie Path = /auth (not /auth/refresh) -->
<!-- 2. GET /campaigns: offset pagination override -->
<!-- 3. Per-User Recipients (Phase 4) -->
<!-- 4. Campaign List Pagination: Offset over Cursor (Phase 4) -->
<!--  -->
<!-- DOC-03 requires 4-state machine, index choices, async queue, open-tracking -->
<!-- (JWT split rationale is already partially covered by the cookie Path section) -->
<!--  -->
<!-- STATE.md accumulated context for "How I Used Claude Code": -->
<!-- Tasks delegated: monorepo scaffolding, Sequelize migrations, auth middleware, -->
<!--   BullMQ queue setup, frontend pages -->
<!-- Corrections: Plan 01-02 corepack shim issue, Plan 02-03 pgcrypto migration prefix, -->
<!--   Phase 3 cookie Path deviation -->
<!-- Out of bounds: modify .docs/requirements.md, re-open locked decisions (Redux+RQ, -->
<!--   BullMQ, split-token JWT, cursor pagination, pixel endpoint, flat monorepo) -->
<!-- Real prompts: available in .planning/phases/*/CONTEXT.md files -->
<!--  -->
<!-- Demo user from Phase 2 seed: -->
<!--   email: demo@example.com, password: password123 (or similar — check seeder) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append 4 sections to docs/DECISIONS.md</name>
  <files>docs/DECISIONS.md</files>
  <read_first>
    - docs/DECISIONS.md (current file — read before appending; preserve existing 4 sections)
    - backend/src/db/config.cjs (dialect, ssl settings)
    - .planning/ROADMAP.md Phase 2 context (index rationale)
    - .planning/ROADMAP.md Phase 5 context (BullMQ rationale)
    - .planning/ROADMAP.md Phase 6 context (tracking pixel rationale)
  </read_first>
  <action>
Append four new sections to docs/DECISIONS.md after the existing content. Do NOT remove or modify the existing sections.

Append exactly:

---

## 4-State Campaign Machine (draft → scheduled → sending → sent)

**Decision:** Campaign status follows a strict four-state machine enforced atomically at the database layer via `UPDATE campaigns SET status = 'sending' WHERE id = $1 AND status IN ('draft', 'scheduled')`. The API returns 409 Conflict when the guard matches zero rows.

**Why four states instead of three:** A three-state machine (draft → sending → sent) cannot represent a campaign that has been committed to send at a future time but has not yet started sending. `scheduled` is the state where `scheduled_at` is set and a BullMQ delayed job is queued. If the BullMQ job fires and the campaign has been deleted or edited in the interim, the worker re-checks status on entry and bails cleanly — no orphaned state.

**Why atomic guard matters:** Two concurrent `POST /campaigns/:id/send` requests can race. Without the `WHERE status IN (...)` guard, both read `draft`, both write `sending`, and both enqueue a worker job — the campaign is processed twice. The atomic `UPDATE ... RETURNING` returns `[affectedCount]`; if the count is 0, the race was lost and the handler returns 409. This is verified by the TEST-02 concurrent-send test.

**Why 409 and not 400:** 409 Conflict is the semantically correct HTTP status for a state machine guard violation — the request is syntactically valid but conflicts with the current resource state. 400 would imply the client sent malformed input.

**References:**
- CAMP-04, CAMP-05, CAMP-06, CAMP-07 in REQUIREMENTS.md
- TEST-02 (concurrent-send atomicity) in REQUIREMENTS.md
- backend/src/services/campaignService.ts (triggerSend guard)

---

## Index Choices

**Decision:** Two explicit composite indexes were created beyond the auto-generated unique and primary key indexes.

1. `idx_campaigns_created_by_created_at_id` on `campaigns(created_by, created_at DESC, id DESC)` — supports `GET /campaigns` which filters by `created_by = req.user.id` and orders by `(created_at DESC, id DESC)`. The three-column index allows Postgres to satisfy the filter + sort in a single B-tree scan with no file sort.

2. `idx_campaign_recipients_campaign_id_status` on `campaign_recipients(campaign_id, status)` — supports `GET /campaigns/:id/stats` which runs `COUNT(*) FILTER (WHERE status = 'sent')` etc. The index allows Postgres to aggregate by scanning the index directly (index-only scan on supported Postgres versions) rather than the full heap.

**What was NOT indexed (and why):**
- `users.email` and `recipients.email` — covered by `UNIQUE` constraints which auto-create B-tree unique indexes in Postgres. A duplicate explicit index would waste write amplification (Pitfall 9 in the research notes).
- `campaign_recipients.tracking_token` — covered by `UNIQUE` constraint for the same reason.
- `campaign_recipients(campaign_id, recipient_id)` — the composite primary key already creates a unique index on those columns.

**References:**
- DATA-02 in REQUIREMENTS.md
- backend/src/migrations/ (indexes created in migration files)

---

## Async Queue Design (BullMQ)

**Decision:** Email sending is delegated to a BullMQ worker that processes jobs from a Redis queue. The HTTP handler transitions the campaign to `sending` atomically, enqueues a job, and returns 202 Accepted immediately. The worker runs inside a Sequelize transaction and transitions the campaign to `sent` when all recipients are processed.

**Why a queue and not synchronous processing:** Email sending (even simulated) is I/O-bound and can be slow. Blocking the HTTP response thread until all recipients are processed would degrade API responsiveness and prevent proper 202 semantics. The queue also enables delayed scheduling (`POST /campaigns/:id/schedule`): BullMQ's `delay` option holds the job until the scheduled timestamp.

**Why separate IORedis connections for Queue and Worker:** BullMQ's documentation requires separate connections for the queue (producer) and the worker (consumer). A single shared connection can cause deadlocks because the worker uses Redis blocking commands (`BRPOP`) that would block the queue's ability to add jobs. Both connections use `maxRetriesPerRequest: null` — the BullMQ default, required to prevent silent job hangs under load (Pitfall C5).

**Worker correctness guarantees:**
- Status re-check on entry: if the campaign is no longer `sending` when the job fires (deleted, or stale delayed job), the worker returns without touching recipient rows.
- Transaction wrapping: all recipient status updates and the campaign `sent` transition are in one Sequelize transaction — partial state on crash is impossible.
- `worker.on('failed')` and `worker.on('error')` handlers log via pino — no silent failures.

**References:**
- QUEUE-01..04 in REQUIREMENTS.md
- backend/src/lib/queue.ts, backend/src/services/sendWorker.ts

---

## Open-Tracking Pixel Design

**Decision:** `GET /track/open/:trackingToken` serves a 43-byte transparent GIF89a and runs an idempotent `UPDATE campaign_recipients SET opened_at = NOW() WHERE tracking_token = $1 AND opened_at IS NULL`. The endpoint is public (no auth) and **always returns 200 + GIF** regardless of whether the token matches any row.

**Why the tracking_token column instead of the composite PK:** The composite primary key `(campaign_id, recipient_id)` is composed of BIGINT values. Embedding `?campaign_id=1&recipient_id=42` in pixel URLs would allow enumeration of all campaign-recipient combinations via sequential integer scanning. The `tracking_token UUID` column provides 122 bits of entropy — brute force enumeration is computationally infeasible.

**Why always-200 (oracle defense):** If the endpoint returned 404 for an invalid token, an attacker could confirm which tokens are valid by observing the response code. With always-200, the caller learns nothing about token validity — even a completely garbage token receives the same pixel response.

**Why idempotent UPDATE (WHERE opened_at IS NULL):** Email clients (and Google's image proxy) may fetch the pixel multiple times. The first fetch sets `opened_at`; subsequent fetches match zero rows and return 200 + GIF with no state change. This ensures `opened_at` always reflects the first open, not the most recent.

**Why a module-scoped pixel buffer:** The 43-byte GIF is allocated once at module load and reused for every request. Allocating a new Buffer per request would be unnecessary GC pressure for a hot, stateless endpoint.

**References:**
- TRACK-01 in REQUIREMENTS.md
- backend/src/routes/track.ts

  </action>
  <verify>
    <automated>grep -c '4-State Campaign Machine' /Users/thalos/Work/ai-fullstack-challenge/docs/DECISIONS.md && grep -c 'Index Choices' /Users/thalos/Work/ai-fullstack-challenge/docs/DECISIONS.md && grep -c 'Async Queue Design' /Users/thalos/Work/ai-fullstack-challenge/docs/DECISIONS.md && grep -c 'Open-Tracking Pixel Design' /Users/thalos/Work/ai-fullstack-challenge/docs/DECISIONS.md</automated>
  </verify>
  <done>
    - docs/DECISIONS.md has all 4 new sections: 4-State Campaign Machine, Index Choices, Async Queue Design, Open-Tracking Pixel Design
    - Existing 4 sections (JWT cookie Path, offset pagination override, per-user recipients, campaign list pagination) are preserved
    - Each new section references the relevant REQUIREMENTS.md IDs
  </done>
</task>

<task type="auto">
  <name>Task 2: Create README.md (full content with drafted Claude Code section)</name>
  <files>README.md</files>
  <read_first>
    - .planning/STATE.md (accumulated context for Claude Code section — decisions, corrections, deferred items)
    - .planning/ROADMAP.md (phase list for delegation context)
    - backend/src/db/config.cjs (confirm demo user credentials are via seeder)
    - .env.example (env vars to document)
  </read_first>
  <action>
Create `README.md` at the repo root. Before committing, draft the "How I Used Claude Code" section from STATE.md and git history context (phases, corrections logged, deferred items).

The README must contain ALL of the following sections:

```markdown
# Mini Campaign Manager

A simplified MarTech tool for creating, managing, scheduling, sending (simulated), and tracking email campaigns. Built as a senior full-stack interview deliverable.

**Stack:** Express 4 + Sequelize 6 + PostgreSQL 16 + BullMQ 5 + Redis | React 18 + Vite 5 + Redux Toolkit + React Query | nginx reverse-proxy | Docker Compose

## Quick Start

### One-command setup (Docker)

Prerequisites: Docker Desktop (or Docker Engine + Compose plugin)

```bash
# 1. Clone the repo
git clone <repo-url>
cd ai-fullstack-challenge

# 2. Copy and configure env
cp .env.example .env
# Edit .env: fill in JWT_ACCESS_SECRET and JWT_REFRESH_SECRET with random 48-char strings
# Generate: openssl rand -base64 48

# 3. Start the full stack
docker compose up --build

# 4. Seed demo data (in a second terminal)
docker compose exec api yarn db:seed

# 5. Open in browser
open http://localhost:8080
```

Everything runs on a single port. No CORS. No manual API URL configuration.

### Demo credentials

After running `docker compose exec api yarn db:seed`:

| Field    | Value              |
|----------|--------------------|
| Email    | demo@example.com   |
| Password | password123        |

The seed creates 1 user, 10 recipients, and 3 campaigns (draft / scheduled / sent).

## Environment Variables

Copy `.env.example` to `.env` and configure:

| Variable | Required | Description |
|----------|----------|-------------|
| `JWT_ACCESS_SECRET` | Yes | ≥32 char random string. Generate: `openssl rand -base64 48` |
| `JWT_REFRESH_SECRET` | Yes | ≥32 char random string, different from ACCESS_SECRET |
| `DATABASE_URL` | Local dev only | Postgres URL. Docker overrides to use service name `postgres`. |
| `REDIS_URL` | Local dev only | Redis URL. Docker overrides to use service name `redis`. |
| `ACCESS_TOKEN_TTL` | No | Default: `15m` |
| `REFRESH_TOKEN_TTL` | No | Default: `7d` |
| `BCRYPT_COST` | No | Default: `10` |
| `PORT` | No | Default: `3000` |
| `LOG_LEVEL` | No | Default: `debug` |

Docker Compose overrides `DATABASE_URL` and `REDIS_URL` automatically to use container service names — you only need to set the JWT secrets in `.env` for `docker compose up` to work.

## Running Tests

```bash
# Backend (Vitest + Supertest — requires local Postgres + Redis running)
yarn workspace @campaign/backend test

# Frontend (Vitest + @testing-library/react)
yarn workspace @campaign/frontend test
```

Backend tests require `DATABASE_URL` (default local Postgres) and `REDIS_URL` (default local Redis) set in the environment. Tests truncate all tables before each test and run serially (`singleFork: true`).

## Developer HMR Flow (optional)

For frontend hot-reload development against the Docker API:

```bash
# 1. Start the Docker stack (api + postgres + redis, but not web)
docker compose up postgres redis api

# 2. In a separate terminal, start the Vite dev server
yarn workspace @campaign/frontend dev
# Opens at http://localhost:5173
# Vite proxies /api/* and /track/* to http://localhost:3000 (the Docker api container)
```

Note: The api container does NOT expose port 3000 to the host in the default compose file.
For HMR, either:
- Run the backend locally: `yarn workspace @campaign/backend dev` (requires local Postgres + Redis)
- Or temporarily add `ports: ["3000:3000"]` to the api service in docker-compose.yml (revert before committing)

## Corepack Note (for local development without Docker)

This project uses Yarn 4 (Berry). If you have Homebrew's classic Yarn 1.x on your PATH, it will shadow Yarn 4.

```bash
# One-time setup: enable corepack to activate Yarn 4 via package.json#packageManager
corepack enable
# Verify
yarn --version  # should be 4.x
```

## Architecture Highlights

- **Campaign state machine** (`draft → scheduled → sending → sent`) enforced via atomic `UPDATE … WHERE status IN (…)` with 409 on guard failure — tested by concurrent-send test
- **Split-token JWT** — short-lived access token in memory + long-lived refresh token in httpOnly SameSite cookie + Redis denylist on logout
- **Stats always SQL** — `COUNT(*) FILTER (WHERE status = 'sent')` aggregate, never computed in JS
- **Open tracking oracle defense** — tracking pixel always returns 200 + 43-byte GIF regardless of token validity
- **Cursor pagination** on recipients, offset pagination on campaigns (documented in `docs/DECISIONS.md`)

See `docs/DECISIONS.md` for detailed rationale on each architectural choice.

## How I Used Claude Code

### What was delegated

The following tasks were fully delegated to Claude Code (via the `/gsd-plan-phase` + `/gsd-execute-phase` GSD workflow):

- **Monorepo scaffolding** (Phase 1): Yarn 4 workspace setup, tsconfig hierarchy, ESLint flat config, Prettier, pino logger
- **Database schema** (Phase 2): All Sequelize migrations — FK ordering, pgcrypto extension, UUID tracking tokens, composite indexes, ENUM types with Postgres-safe down() migrations
- **Auth middleware** (Phase 3): Split-token JWT primitives, bcrypt timing defense, Redis denylist, cookie rotation, `authenticate` middleware, `buildApp()` factory split
- **CRUD endpoints** (Phase 4): Campaign and recipient services, offset + cursor pagination, single-SQL stats aggregate, smoke scripts
- **BullMQ queue** (Phase 5): Queue/Worker IORedis wiring, atomic send guard, transaction-wrapped simulation, delayed scheduling
- **Tracking pixel** (Phase 6): Module-scoped GIF buffer, idempotent UPDATE, always-200 oracle defense
- **Backend tests** (Phase 7): Vitest + Supertest test infrastructure, four test files covering status guards, send atomicity, stats, auth boundaries
- **Frontend foundation** (Phase 8): Vite + React + Redux + React Query wiring, memoized refresh interceptor, bootstrap sequence
- **Frontend pages** (Phase 9): Login, campaign list (infinite scroll), new campaign form (email tokenizer), detail page (polling, action buttons, progress bars)
- **Docker wiring** (Phase 10): Multi-stage Dockerfiles, nginx.conf, docker-compose.yml service graph

### Real prompts used

**Phase 3 (Auth):**
> "Plan Phase 3. The refresh cookie Path should be /auth not /auth/refresh — I want /auth/logout to be able to read and clear it so it can denylist the jti in Redis."

This was a user decision locked before planning. Claude's research had defaulted to `/auth/refresh`; the user corrected this before plan creation.

**Phase 7 (Backend tests):**
> "Plan Phase 7. The concurrent-send test must actually hit a real Postgres and fire two Promise.all requests, not mock anything. I want proof the atomic guard works."

**Phase 9 (Frontend pages):**
> "Execute plan 09-03. The campaign list uses offset pagination with page numbers (not cursor infinite scroll) — the CONTEXT.md decision D-17 says this. Do not implement cursor pagination here."

### Where Claude Code needed correction

1. **Plan 01-02 — corepack shim:** Claude's initial Dockerfile draft used `yarn` directly, which would invoke Homebrew's classic Yarn 1.x on developer machines. Corrected to use the corepack shim at `/usr/local/bin/yarn` with an explicit `corepack enable` step. Carried forward as a README note.

2. **Plan 02-03 — pgcrypto migration prefix:** Claude initially generated the pgcrypto migration with a timestamp prefix. Corrected to use a `00000000000000-` numeric prefix so it always sorts first lexically and runs before any other migration, even if a future migration gets a pre-2026 timestamp by accident.

3. **Plan 03-03 — cookie Path deviation:** Claude's auth research defaulted to `Path=/auth/refresh`. The user caught that `/auth/logout` would then be unable to read the cookie to denylist its jti — a correctness failure. Corrected to `Path=/auth`. Claude updated DECISIONS.md with the full rationale.

### What Claude Code was NOT allowed to do

- **Modify `.docs/requirements.md`** — this file is the reviewer's original spec kept verbatim as the source-of-truth anchor. Claude was instructed to read it, never write it.
- **Re-open locked decisions** — once choices like Redux+React Query (not Zustand/SWR), BullMQ (not pg-boss or Agenda), split-token JWT, cursor pagination on recipients, and the flat monorepo structure were decided, Claude was not allowed to re-litigate them even when research suggested alternatives.
- **Add v2 features** — `docs/REQUIREMENTS.md` has a v2 deferred list (real SMTP, rich-text editor, unsubscribe). Claude was not allowed to pull from this list.
- **Reconstruct the AI collaboration log from memory** — the "How I Used Claude Code" section (this section) was drafted from the live STATE.md log and git history captured during the build, not reconstructed after the fact.
```

After writing README.md with this full content (including the drafted "How I Used Claude Code" section), STOP and present the "How I Used Claude Code" section to the user. Do NOT commit README.md yet.

Present the section with this exact message:
"README.md has been written. Before committing, please review the 'How I Used Claude Code' section above. This section must reflect your actual experience — please correct any inaccuracies, add real prompts you remember, adjust the corrections list, or modify the out-of-bounds list. Type 'approved' when the section is ready to commit, or provide corrections."
  </action>
  <verify>
    <automated>grep -c 'docker compose up' /Users/thalos/Work/ai-fullstack-challenge/README.md && grep -c 'How I Used Claude Code' /Users/thalos/Work/ai-fullstack-challenge/README.md && grep -c 'demo@example.com' /Users/thalos/Work/ai-fullstack-challenge/README.md</automated>
  </verify>
  <done>
    README.md exists and contains:
    - "docker compose up" (setup section)
    - "How I Used Claude Code" section heading
    - "demo@example.com" demo credentials
    File is written but NOT yet committed (awaiting user review of Claude Code section)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
README.md has been written with all required sections:
- Quick Start (docker compose up)
- Demo credentials
- Environment variables table
- Test running instructions
- Developer HMR flow
- Architecture highlights
- How I Used Claude Code (tasks delegated, real prompts, corrections, out-of-bounds list)

docs/DECISIONS.md has been updated with 4 new sections:
- 4-State Campaign Machine
- Index Choices
- Async Queue Design (BullMQ)
- Open-Tracking Pixel Design
  </what-built>
  <how-to-verify>
1. Read the "How I Used Claude Code" section in README.md
2. Verify it accurately reflects your build experience:
   - Are the delegated tasks correct?
   - Are the 3 real prompts representative (or do you have better examples)?
   - Are the 3 corrections accurate (corepack shim, pgcrypto prefix, cookie Path)?
   - Is the out-of-bounds list complete and accurate?
3. Correct any inaccuracies directly in README.md if needed
4. Also spot-check docs/DECISIONS.md — the 4 new sections should be technically accurate
  </how-to-verify>
  <resume-signal>Type "approved" to commit README.md and docs/DECISIONS.md, or describe corrections to make first</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README.md → public repo | Reviewer reads this; must not contain secrets or incorrect instructions |
| docs/DECISIONS.md → public repo | Must accurately represent technical choices made |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10 | Information Disclosure | README credentials | accept | Demo credentials (demo@example.com / password123) are intentionally public for reviewer walkthrough |
| T-10-11 | Tampering | "How I Used Claude Code" authenticity | mitigate | User reviews and approves before commit; checkpoint gate enforces this |
</threat_model>

<verification>
After user approves and docs are committed:
1. `grep -c "4-State Campaign Machine" docs/DECISIONS.md` — expect 1
2. `grep -c "Index Choices" docs/DECISIONS.md` — expect 1
3. `grep -c "Async Queue Design" docs/DECISIONS.md` — expect 1
4. `grep -c "Open-Tracking Pixel Design" docs/DECISIONS.md` — expect 1
5. `grep -c "How I Used Claude Code" README.md` — expect 1
6. `grep -c "docker compose up" README.md` — expect ≥1
7. `grep -c "demo@example.com" README.md` — expect 1
8. `git log --oneline -3` — confirm both files appear in commit
</verification>

<success_criteria>
- docs/DECISIONS.md has all 4 new sections (4-state machine, index choices, async queue, open-tracking)
- README.md exists with one-command setup, demo creds, env vars, test instructions, HMR flow note, architecture highlights
- "How I Used Claude Code" section in README.md approved by user before commit
- Both files committed to git after user approval
</success_criteria>

<output>
After completion, create `.planning/phases/10-full-docker-stack-integration-docs/10-04-SUMMARY.md`
</output>
